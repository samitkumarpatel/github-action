inputs:
  name: 
    description: 'Your name'
    default: 'No name provided'
runs:
  using: "composite"
  steps:
    - name: message 
      run: |
        echo Hello ${{ inputs.name }}
      shell: bash

