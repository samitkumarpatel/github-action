name: Output Example One

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.step1.outputs.test }}
    steps:
    - name: Shell Output
      id: shell_output
      run: echo "Hello World from shell_output" >> $GITHUB_OUTPUT
  print_output:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Shell Output Print
      run: echo ${{ needs.build.outputs.output1 }}
